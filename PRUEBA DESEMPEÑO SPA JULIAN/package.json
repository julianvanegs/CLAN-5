{
  "name": "support-tickets",
  "version": "1.0.0",
  "description": "Support ticket management system",
  "scripts": {
    "auth": "json-server --watch auth-db.json --port 3001",
    "data": "json-server --watch data-db.json --port 3002",
    "dev": "npx live-server public --port=8080"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "json-server": "^0.17.4"
  }
}
